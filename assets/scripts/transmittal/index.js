$(document).ready(function () {
    // Initialize tables
    initTables();
    
    // Handle URL hash changes and set initial active tab
    handleHashAndTabs();
    
    // Initialize event listeners
    initEventListeners();
}); 